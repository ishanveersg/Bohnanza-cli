#include <iostream>
#include "Blue.h"
#include "Chili.h"
#include "Stink.h"
#include "Green.h"
#include "Soy.h"
#include "Black.h"
#include "Red.h"
#include "Garden.h"
#include "Card.h"
/*Blue, Chili, Stink, Green, Soy, Black, Red, Garden*/


/* run this program using the console pauser or add your own getch, system("pause") or input loop */

class Chain{
	private:
		int nombre;
	public:
		Chain(istream&, const CardFactory* );
		void Chain<T>& operator+=( Card* );
		int sell();
};

Chain::Chain(*Machain, CardFactory){
	Chain = &Machaine;
	nombre = CardFactory;	
}

Chain operator += () { // notation préfixée
	Card = new Card(c,coins);
}
//A verifier
int Chain::sell(){
	nombre = sizeof(Chain);
	Card::getCardsPerCoin(nombre)
}

